// todo, when we have deeplinking, define the scheme there and use it in this string
export const AUTH_REDIRECT_URI = "limbo://auth/callback";
export const TOKEN_REFRESH_THRESHOLD_MINUTES = 10; // how long before expiration to refresh the token
